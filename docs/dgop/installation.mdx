---
title: Installation
sidebar_position: 2
---

import Hero from '@site/src/components/Hero';

<Hero
  asciiArt={`██████╗  ██████╗  ██████╗ ██████╗
██╔══██╗██╔════╝ ██╔═══██╗██╔══██╗
██║  ██║██║  ███╗██║   ██║██████╔╝
██║  ██║██║   ██║██║   ██║██╔═══╝
██████╔╝╚██████╔╝╚██████╔╝██║
╚═════╝  ╚═════╝  ╚═════╝ ╚═╝
                                  `}
/>

# DGOP Installation

`dgop` has zero dependencies and can be compiled to a single binary.

### Distribution Packages

#### Arch Linux (AUR)
```bash
paru -S dgop-bin
```

#### Fedora/RHEL
```bash
sudo dnf copr enable avengemedia/danklinux
sudo dnf install dgop
```

### Pre-built Binaries

Download the latest release for your architecture:

```bash
# Download and install
wget https://github.com/AvengeMedia/dgop/releases/latest/download/dgop-linux-amd64.gz
gunzip dgop-linux-amd64.gz
chmod +x dgop-linux-amd64
sudo mv dgop-linux-amd64 /usr/local/bin/dgop
```

### From Source

Requirements: Go 1.24+

```bash
git clone https://github.com/AvengeMedia/dgop
cd dgop
make
sudo make install
```

## Verification

Test the installation:

```bash
# Check version
dgop version

# Get system metrics
dgop system

# Run API server
dgop server

# See available commands
dgop --help
```

## System Requirements

- **Operating System**: Linux (uses `/proc` and `/sys` filesystems)
- **Go Version**: 1.24+ (for building from source)

### Optional Dependencies

For enhanced functionality:

- `nvidia-smi` - NVIDIA GPU monitoring
- `pciutils (lspci)` - PCI device information

## Integration with DMS

If you have DankMaterialShell installed, installing dgop will enable additional features:

- **System Widgets**: CPU, RAM, GPU, Disk, and Network widgets
- **Process Monitor**: Process overview sorted by cpu/ram usage

## Next Steps

- [Configuration](configuration) - Configure dgop
- [Usage](usage) - Learn CLI commands and API usage
