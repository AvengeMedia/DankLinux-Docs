---
title: 配置
description: 配置 DankGreeter 登录屏幕外观
sidebar_position: 4
---

import Hero from '@site/src/components/Hero';

<Hero
  asciiArt={`██████╗  █████╗ ███╗   ██╗██╗  ██╗ ██████╗ ██████╗ ███████╗███████╗████████╗
██╔══██╗██╔══██╗████╗  ██║██║ ██╔╝██╔════╝ ██╔══██╗██╔════╝██╔════╝╚══██╔══╝
██║  ██║███████║██╔██╗ ██║█████╔╝ ██║  ███╗██████╔╝█████╗  █████╗     ██║
██║  ██║██╔══██║██║╚██╗██║██╔═██╗ ██║   ██║██╔══██╗██╔══╝  ██╔══╝     ██║
██████╔╝██║  ██║██║ ╚████║██║  ██╗╚██████╔╝██║  ██║███████╗███████╗   ██║
╚═════╝ ╚═╝  ╚═╝╚═╝  ╚═══╝╚═╝  ╚═╝ ╚═════╝ ╚═╝  ╚═╝╚══════╝╚══════╝   ╚═╝
                                                                            `}
/>

# 配置 {#configuration}

## 自定义合成器配置 {#custom-compositor-configuration}

:::tip

自定义合成器配置允许您专门为 greeter 配置显示器（分辨率、刷新率、位置等）。

:::

### niri {#niri}

1. 在 `/etc/greetd/niri.kdl` 创建基准配置文件：
  ```bash
  sudo tee /etc/greetd/niri.kdl > /dev/null << 'EOF'
  hotkey-overlay {
      skip-at-startup
  }

  environment {
      DMS_RUN_GREETER "1"
  }

  gestures {
    hot-corners {
      off
    }
  }

  layout {
    background-color "#000000"
  }
  EOF
  ```

2. 更新 `/etc/greetd/config.toml` 中的命令以使用此配置
  ```toml
  #...existing configuration
  command = "dms-greeter --command niri -C /etc/greetd/niri.kdl"
  #...existing configuration
  ```

3. 根据您的需要编辑 `/etc/greetd/niri.kdl`，greeter 将在此合成器配置下运行

### Hyprland {#hyprland}

1. 在 `/etc/greetd/hypr.conf` 创建基准配置文件：
  ```bash
  sudo tee /etc/greetd/hypr.conf > /dev/null << 'EOF'
  env = DMS_RUN_GREETER,1

  misc {
      disable_hyprland_logo = true
  }
  EOF
  ```

2. 更新 `/etc/greetd/config.toml` 中的命令以使用此配置
  ```toml
  #...existing configuration
  command = "dms-greeter --command hyprland -C /etc/greetd/hypr.conf"
  #...existing configuration
  ```

3. 根据您的需要编辑 `/etc/greetd/hypr.conf`，greeter 将在此合成器配置下运行


### Sway {#sway}

1. 在 `/etc/greetd/sway` 创建基准配置文件：
  ```bash
  sudo tee /etc/greetd/sway > /dev/null << 'EOF'
  # Sway greeter configuration
  # The exec command to launch the greeter is automatically appended by dms-greeter
  EOF
  ```

2. 更新 `/etc/greetd/config.toml` 中的命令以使用此配置
  ```toml
  #...existing configuration
  command = "dms-greeter --command sway -C /etc/greetd/sway"
  #...existing configuration
  ```

3. 根据您的需要编辑 `/etc/greetd/sway`，greeter 将在此合成器配置下运行

### Mango {#mango}

1. 在 `/etc/greetd/mango.conf` 创建基准配置文件：
  ```bash
  sudo tee /etc/greetd/mango.conf > /dev/null << 'EOF'
  # Mango greeter configuration
  EOF
  ```

2. 更新 `/etc/greetd/config.toml` 中的命令以使用此配置
  ```toml
  #...existing configuration
  command = "dms-greeter --command mango -C /etc/greetd/mango.conf"
  #...existing configuration
  ```

3. 根据您的需要编辑 `/etc/greetd/mango.conf`，greeter 将在此合成器配置下运行

### Miracle WM {#miracle-wm}

1. 在 `/etc/greetd/miracle-wm.yaml` 创建基准配置文件：
  ```bash
  sudo tee /etc/greetd/miracle-wm.yaml > /dev/null << 'EOF'
  # Miracle WM greeter configuration
  EOF
  ```

2. 更新 `/etc/greetd/config.toml` 中的命令以使用此配置
  ```toml
  #...existing configuration
  command = "dms-greeter --command miracle-wm -C /etc/greetd/miracle-wm.yaml"
  #...existing configuration
  ```

3. 根据您的需要编辑 `/etc/greetd/miracle-wm.yaml`，greeter 将在此合成器配置下运行

## 与 DMS 同步 {#syncing-with-dms}

您可以自动将系统 greeter 与登录用户的壁纸、主题、字体等同步。

### 自动同步 {#automatic-sync}

您可以使用 `dms greeter sync` 自动设置主题同步。此命令适用于：
- dankinstall 用户
- Arch AUR 用户
- Fedora COPR 用户

:::note

**NixOS 用户：**`dms greeter sync` 命令在 NixOS 上不可用。请按照下面的手动同步步骤操作。

:::

```bash
dms greeter sync
```

这将：
- 将您的用户添加到 `greeter` 组（如果需要）
- 为 greeter 访问设置父目录的 ACL 权限
- 配置 DMS 配置目录的组权限
- 创建符号链接以同步设置、壁纸和颜色主题

:::note

运行 `dms greeter sync` 后，您需要注销并重新登录，组成员身份更改才能生效。重新登录后，您的 greeter 将自动反映您当前的 DMS 主题、壁纸和设置。

:::

### 手动同步 {#manual-sync}

如果您更愿意手动设置同步：

1. 将您的用户添加到 `greeter` 组：
  ```bash
  sudo usermod -aG greeter <username>
  ```

2. 为父目录设置 ACL 权限：
  ```bash
  setfacl -m u:greeter:x ~
  setfacl -m u:greeter:x ~/.config
  setfacl -m u:greeter:x ~/.local
  setfacl -m u:greeter:x ~/.cache
  setfacl -m u:greeter:x ~/.local/state
  ```

3. 设置 DMS 配置目录的组权限：
  ```bash
  sudo chgrp -R greeter ~/.config/DankMaterialShell
  sudo chmod -R g+rX ~/.config/DankMaterialShell

  sudo chgrp -R greeter ~/.local/state/DankMaterialShell
  sudo chmod -R g+rX ~/.local/state/DankMaterialShell

  sudo chgrp -R greeter ~/.cache/quickshell
  sudo chmod -R g+rX ~/.cache/quickshell
  ```

4. 创建配置符号链接：
  ```bash
  sudo ln -sf ~/.config/DankMaterialShell/settings.json /var/cache/dms-greeter/settings.json
  sudo ln -sf ~/.local/state/DankMaterialShell/session.json /var/cache/dms-greeter/session.json
  sudo ln -sf ~/.cache/quickshell/dankshell/dms-colors.json /var/cache/dms-greeter/colors.json
  ```

:::note

您必须注销或重新启动才能使组更改生效。

:::

## 检查同步状态 {#checking-sync-status}

要验证您的 greeter 是否已正确配置并与您的用户设置同步，请运行：

```bash
dms greeter status
```

此命令适用于所有用户，将检查：
- ✓ 组成员身份（您的用户是否在 `greeter` 组中）
- ✓ 缓存目录是否存在
- ✓ 配置符号链接（设置、壁纸、颜色）
- ✓ 源文件是否存在且可读

### 示例输出 {#example-output}

```
=== DMS Greeter Status ===

Group Membership:
  ✓ User is in greeter group

Greeter Cache Directory:
  ✓ /var/cache/dms-greeter exists

Configuration Symlinks:
  ✓ Settings: synced correctly
  ✓ Session state: synced correctly
  ✓ Color theme: synced correctly

✓ All checks passed! Greeter is properly configured.
```

### 故障排除 {#troubleshooting}

如果 `dms greeter status` 显示问题：

**用户不在 greeter 组中：**
```bash
dms greeter sync
# Then log out and log back in
```

**符号链接缺失或损坏：**
```bash
dms greeter sync
```

**ACL 权限问题（主题/壁纸未显示）：**
```bash
# Check if ACLs are set
getfacl ~ ~/.config ~/.local ~/.cache ~/.local/state | grep "user:greeter"

# If not, run sync again
dms greeter sync
```

## 手动配置 {#manual-configuration}

greeter 使用位于 `/var/cache/dms-greeter/`（或您自定义的 `DMS_GREET_CFG_DIR`）中的三个主要配置文件：

- `settings.json` - 外观、行为和小部件设置
- `session.json` - 壁纸配置
- `colors.json` - 配色方案配置

:::note

这些文件对 greeter 是只读的，不会被写入或更新。

:::

### 颜色 {#colors}

颜色的配置方式与 [DankMaterialShell 自定义主题](/docs/dankmaterialshell/custom-themes) 中描述的完全相同

### 设置 {#settings}

`settings.json` 文件控制 greeter 的外观和行为。以下是完整参考：

#### 时钟和时间 {#clock--time}

```json
{
  "use24HourClock": true,
  "showSeconds": false,
  "lockDateFormat": ""
}
```

- `use24HourClock`：使用 24 小时格式（`true`）或 12 小时格式（`false`）
- `showSeconds`：在时钟中显示秒数
- `lockDateFormat`：自定义 Qt 日期格式字符串（空 = `Locale.LongFormat`）

#### 天气 {#weather}

```json
{
  "weatherEnabled": true,
  "weatherLocation": "New York, NY",
  "weatherCoordinates": "40.7128,-74.0060",
  "useAutoLocation": false,
  "useFahrenheit": false
}
```

- `weatherEnabled`：在右上角显示天气小部件
- `weatherLocation`：位置的显示名称
- `weatherCoordinates`：`"纬度,经度"` 字符串
- `useAutoLocation`：通过 IP 地理位置自动检测位置
- `useFahrenheit`：使用华氏度（`true`）或摄氏度（`false`）

#### 外观 {#appearance}

```json
{
  "currentThemeName": "blue",
  "customThemeFile": "",
  "matugenScheme": "scheme-tonal-spot",
  "iconTheme": "System Default",
  "fontFamily": "Inter Variable",
  "fontWeight": 400,
  "fontScale": 1.0,
  "cornerRadius": 12,
  "widgetBackgroundColor": "sch",
  "surfaceBase": "s",
  "animationSpeed": 2
}
```

- `currentThemeName`：内置主题名称（`"blue"`、`"red"`、`"green"` 等）
- `customThemeFile`：自定义 matugen JSON 主题文件的路径
- `matugenScheme`：Matugen 配色方案变体
- `iconTheme`：用于 DankIcon 回退的图标主题名称
- `fontFamily`：主字体系列
- `fontWeight`：字重（`400` = 常规，`700` = 粗体）
- `fontScale`：字号倍数（`1.0` = 默认）
- `cornerRadius`：UI 元素的圆角半径（像素）
- `widgetBackgroundColor`：小部件背景方案（`"sch"`、`"s"`、`"sv"`）
- `surfaceBase`：表面基础配色方案（`"s"`、`"sv"`、`"sb"`）
- `animationSpeed`：动画速度（`0` = 最快，`4` = 最慢，`2` = 默认）

#### 完整示例 {#complete-example}

`/var/cache/dms-greeter/settings.json`：

```json
{
  "use24HourClock": true,
  "showSeconds": false,
  "lockDateFormat": "",
  "lockScreenShowPowerActions": true,
  "useFahrenheit": false,
  "weatherLocation": "New York, NY",
  "weatherCoordinates": "40.7128,-74.0060",
  "useAutoLocation": false,
  "weatherEnabled": true,
  "currentThemeName": "blue",
  "customThemeFile": "",
  "matugenScheme": "scheme-tonal-spot",
  "nightModeEnabled": false,
  "iconTheme": "System Default",
  "fontFamily": "Inter Variable",
  "fontWeight": 400,
  "fontScale": 1.0,
  "cornerRadius": 12,
  "widgetBackgroundColor": "sch",
  "surfaceBase": "s",
  "animationSpeed": 2
}
```

### 壁纸 {#wallpapers}

`session.json` 文件控制壁纸设置。

#### 基本配置 {#basic-configuration}

```json
{
  "wallpaperPath": "/path/to/wallpaper.jpg",
  "wallpaperFillMode": "PreserveAspectCrop"
}
```

- `wallpaperPath`：所有显示器的默认壁纸
- `wallpaperFillMode`：Qt 填充模式
  - `"PreserveAspectCrop"` - 裁剪以填充（默认）
  - `"PreserveAspectFit"` - 保持在边界内
  - `"Stretch"` - 拉伸以填充

#### 每显示器壁纸 {#per-monitor-wallpapers}

```json
{
  "wallpaperPath": "/path/to/default.jpg",
  "wallpaperFillMode": "PreserveAspectCrop",
  "monitorWallpapers": {
    "DP-1": "/path/to/monitor1-wallpaper.jpg",
    "DP-2": "/path/to/monitor2-wallpaper.jpg",
    "HDMI-A-1": "#1a1a1a"
  }
}
```

- `monitorWallpapers`：每显示器壁纸覆盖
  - 键：来自 `niri msg outputs` 或 `hyprctl monitors` 的显示器名称
  - 值格式：
    - 文件路径：`/path/to/image.jpg`
    - 纯色：`#RRGGBB` 格式
    - Wallpaper Engine：`we:workshop_id` 格式

#### 完整示例 {#complete-example-1}

`/var/cache/dms-greeter/session.json`：

```json
{
  "wallpaperPath": "/usr/share/backgrounds/default.jpg",
  "wallpaperFillMode": "PreserveAspectCrop",
  "monitorWallpapers": {
    "DP-1": "/home/user/Pictures/wallpaper-main.png",
    "DP-2": "#2e3440",
    "HDMI-A-1": "/home/user/Pictures/wallpaper-side.jpg"
  }
}
```
