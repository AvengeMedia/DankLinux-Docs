apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dlx-docs-server-uploads
  namespace: danklinux
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: hcloud-volumes
  resources:
    requests:
      storage: 10Gi
